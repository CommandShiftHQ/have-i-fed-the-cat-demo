version: "3"
services:
  express-app:
    scale: 3

  load-balancer:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - express-app
    ports:
      - "3000:3000"
